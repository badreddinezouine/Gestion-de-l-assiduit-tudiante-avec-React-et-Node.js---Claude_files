import React from 'react';
import { Container, Typography, Box } from '@mui/material';

const AttendanceQR = () => {
  return (
    <Container>
      <Box sx={{ mt: 4 }}>
        <Typography variant="h4">QR Code de Présence</Typography>
        <Typography>Page en développement.</Typography>
      </Box>
    </Container>
  );
};

export default AttendanceQR;